<h1>Listado de Productos</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Precio</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td>
          <a href="/products/<%= product.id %>/edit">Editar</a>
          <a href="/products/<%= product.id %>" data-method="delete">Eliminar</a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<a href="/products/new">Crear nuevo producto</a>


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestión de Productos e Insumos (ABM)</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      color: #333;
      text-align: center;
      margin: 0;
      padding: 0;
    }
    h1 {
      background-color: #007BFF;
      color: white;
      padding: 20px;
      margin: 0;
    }
    .menu {
      margin: 20px auto;
      max-width: 800px;
      padding: 20px;
      background-color: #ffffff;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }
    .menu h2 {
      margin-bottom: 20px;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
    }
    button {
      background-color: #007BFF;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
      transition: background-color 0.3s;
    }
    button:hover {
      background-color: #0056b3;
    }
    a.back {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      color: #007BFF;
      font-size: 14px;
    }
    a.back:hover {
      color: #0056b3;
    }
    .section-title {
      font-size: 18px;
      color: #555;
      margin: 10px 0;
    }
  </style>
</head>
<body>

<h1>Gestión de Productos e Insumos (ABM)</h1>

<div class="menu">
  <h2>Opciones disponibles</h2>

  <!-- Sección de productos -->
  <div class="section-title">Opciones para Productos</div>
  <div class="grid">
    <form action="/products" method="get">
      <button type="submit">Ver Productos</button>
    </form>
    <form action="/products/new" method="get">
      <button type="submit">Crear Producto</button>
    </form>
    <form action="/products" method="get">
      <button type="submit">Modificar Producto</button>
    </form>
  </div>

  <!-- Sección de insumos -->
  <div class="section-title">Opciones para Insumos</div>
  <div class="grid">
    <form action="/inputs" method="get">
      <button type="submit">Ver Insumos</button>
    </form>
    <form action="/inputs/new" method="get">
      <button type="submit">Crear Insumo</button>
    </form>
    <form action="/inputs" method="get">
      <button type="submit">Modificar Insumo</button>
    </form>
  </div>
</div>

<a class="back" href="/">Volver al inicio</a>

</body>
</html>

